/**
 * This file performs the functions called at each resize of the window.
 */

// Import here the functions to use in the listening function of the resize

export function resize() {

  $(window).resize( () => {

    // Place here functions to execute when the user resize the window

  })

}
